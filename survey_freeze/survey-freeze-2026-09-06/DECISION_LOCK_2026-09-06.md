# Decision lock — GPB path without author-mediated ×2

**Date:** 2026-09-06  
**Status:** LOCKED

## Decision

1. **Do not block** the GPB path on author-mediated ×2 archives.  
2. **Do not** lower verification-depth grades to invent directs or pad *n*.  
3. **Primary completion target** = Option B **auditability survey** (microbial genome ML / AMR core) with a frozen denominator + main-text Methods skeleton.  
4. After survey freeze + text alignment + release verification → **submit GPB**.  
5. Author-mediated / figshare permission remain **optional add-ons**, never primary public-direct substitutes.

## Venue story (target)

> We introduce an executable claim–split **reporting profile** and reference implementation for biological block-held-out genome ML evaluation, validate the software, and use a **protocol-defined public-artifact auditability survey** to characterize the practical availability of auditable evaluation artifacts—without equating missing artifacts with split failure.

## Survey completion rule (honest)

| Layer | Frozen / required |
|-------|-------------------|
| Search date | 2026-09-05 (Europe PMC) |
| Universe | Protocol §0–2 (AMR / microbial genome ML core) |
| Working queue | Q2 ∪ Q3 deduped = **3471** (logged); Q1 sensitivity only |
| Enrichment path for deep coding | Explicit biological-block language (Q3/Q4 abstract filters + wave3) — **not** a claim that all 3471 received human T/A |
| Primary denominator for depth bars | Evidence sources with `claim_declared=yes` after dual/strict coding; **one row per evidence_source_id** |
| GeneLab | Adjacent exemplar; report separately; **not** forced into microbial primary denominator |
| Forbidden | Leakage prevalence %; inventing membership; organ-padding GeneLab |

## Still required before “survey complete”

- [ ] Close deep-coding for all `claim_declared=yes` candidates in the enrichment set (wave3_strict + Q4 bio-block AMR + seeds), with pending → terminal depth  
- [ ] Frozen registry CSV + screening flow counts table  
- [ ] Main-text Methods paragraph + SI flow figure aligned to those counts  
- [ ] No `pending_*` depths left in the **primary** denominator rows (copy-number may remain *not-assessable* if figshare stays blocked)

## Explicitly deferred (non-blocking)

- Author-mediated ×2  
- Cross-domain 50–100 paper expansion  
- Nested CV / multi-block executor beyond v0.2 provenance wording  
- Third-party adoption
